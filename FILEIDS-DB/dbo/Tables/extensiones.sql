CREATE TABLE [dbo].[extensiones] (
    [id_extension]        INT           NOT NULL,
    [extension_descripcion]    VARCHAR (255) NOT NULL,
    [extension_extension] VARCHAR (10)  NOT NULL,
    PRIMARY KEY CLUSTERED ([id_extension] ASC)
);

